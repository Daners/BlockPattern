import { ResponsiveCardsDirective } from './responsive-cards.directive';

describe('ResponsiveCardsDirective', () => {
  it('should create an instance', () => {
    const directive = new ResponsiveCardsDirective();
    expect(directive).toBeTruthy();
  });
});
